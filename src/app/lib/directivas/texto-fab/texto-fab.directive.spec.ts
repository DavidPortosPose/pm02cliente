import { TextoFabDirective } from './texto-fab.directive';

describe('TextoFabDirective', () => {
  it('should create an instance', () => {
    const directive = new TextoFabDirective();
    expect(directive).toBeTruthy();
  });
});
